# Final Integrated Trust Boundary

## Verified

- CRC and internal SHA-256 manifests of the ten canonical source ZIPs;
- Core claim, GAP, handoff, summary, and trust-boundary snapshots extracted from the canonical ZIPs;
- Version sequence, node decisions, and hard flags from v0.1 to v1.0;
- Fixed abstract continuous interval certificate of v0.7;
- Abstract $58$-dimensional position product-box interval certificate of v1.0;
- The unified claim register and GAP ledger do not mark any node as a global RH certificate.

## Valid Only Within the Abstract Model

- Clamped $H_0^2$ weak duality;
- Green-kernel projection;
- Low-rank Schur reduction;
- Fixed atomic witness positivity;
- Synthetic occupancy cover;
- Uncertain-location cover of the $58$ inherited axis atoms.

## Not Yet Proven

- Synthetic/inherited atoms are actual $\zeta$ zeros;
- Complete conditional source theorem from a hypothetical off-axis zero to a rational occupancy cell;
- Zeta-facing directed certification of the five bands and tail coefficients;
- Complete explicit-formula admissibility of the clamped test-function class;
- General prime-side nonnegative cone of the same function;
- Exhaustion of all off-axis regions and infinite height;
- RH.

## Role of Floating-Point Computations

Floating-point computations may be used for:

- Searching for candidates;
- Selecting interval targets;
- Comparing methods;
- Finding failure directions;
- Generating exact rational candidates.

Floating-point computations cannot independently support:

- Continuous universal positivity;
- Universal counterexamples;
- Source-theorem coefficients;
- Zeta occupancy;
- Global RH conclusions.

## Canonical source policy

The ten ZIPs are canonical source objects. Extracted working directories serve only for temporary storage and replay, and are not roots of evidence.

During integration, it was discovered that `outputs/gram_results.json` in the extracted v0.2 was truncated, but the canonical v0.2 ZIP is intact and its manifest passes. All v0.2 snapshots and claims in this package are taken from the ZIP.

## Prohibited Inferences

- Abstract interval certificate $\Rightarrow$ actual zeta theorem;
- Scalar count $\Rightarrow$ arbitrary operator occupancy;
- Synthetic occupancy $\Rightarrow$ actual zero presence;
- Finite-family obstruction $\Rightarrow$ all-function impossibility;
- Interval method failure $\Rightarrow$ point counterexample;
- Local product box $\Rightarrow$ global critical-strip cover;
- One arithmetic-positive scalar $\Rightarrow$ general prime-side cone;
- Any current artifact $\Rightarrow$ RH proof or disproof.